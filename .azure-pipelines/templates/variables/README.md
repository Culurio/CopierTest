# Variable Templates

Store parameterized variable templates for secrets, feature flags, and environment-specific configuration. Reference them via `- template: variables/<file>.yml` so updates automatically apply to every consumer pipeline.
