# Job Templates

Reusable job definitions that include an agent pool, checkout strategy, and set of steps. Jobs should accept parameters for inputs such as solution paths or container images and rely on step templates for repeated logic.
