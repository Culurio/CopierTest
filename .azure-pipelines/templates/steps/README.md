# Step Templates

Lowest-level templates composed of Azure DevOps tasks or scripts. Keep them laser-focused (install dependencies, run tests, publish artifacts) so they can be composed into jobs and stages.
