# Stage Templates

Contains templates that describe entire stages (collection of jobs). Use when you need to enforce a standard build/test/release flow while still allowing pipelines to pass parameters such as environment names, approvals, or resource references.
